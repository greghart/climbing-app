import * as React from 'react';
import Route from '../../../models/Route';

interface Props {
  myRoute: Route;
}

const RouteLayoutPhotos: React.SFC<Props> = (props) => {
  return (
    <div>
      Photos will go here.
    </div>
  )
};

export default RouteLayoutPhotos;

